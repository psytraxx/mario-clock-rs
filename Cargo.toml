[package]
name = "super-mario-clock"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = { version = "0.10.0", default-features = false, features = [
    "log",
    "alloc",
] }
esp-hal = { version = "1.1.1", features = ["esp32s3", "log-04", "unstable"] }

esp-rtos = { version = "0.3.0", features = [
    "embassy",
    "esp-alloc",
    "esp-radio",
    "esp32s3",
    "log-04",
] }

esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32s3"] }
log = "0.4.30"

embassy-net = { version = "0.9.1", features = ["dhcpv4", "log", "udp", "dns"] }
esp-alloc = "0.10.0"
esp-backtrace = { version = "0.19.0", features = [
    "esp32s3",
    "panic-handler",
    "println",
] }
esp-println = { version = "0.17.0", features = ["esp32s3", "log-04"] }
# for more networking protocol support see https://crates.io/crates/edge-net
embassy-executor = { version = "0.10.0", features = ["log"] }
embassy-time = { version = "0.5.1", features = ["log"] }
esp-radio = { version = "0.18.0", features = [
    "esp-alloc",
    "esp32s3",
    "log-04",
    "unstable",
    "wifi",
] }

embassy-sync = "0.8.0"
static_cell = "2.1.1"
heapless = "0.9.3"
esp-hub75 = { version = "0.11.0", features = ["esp32s3", "log"] }
embedded-graphics = "0.8.2"
embedded-hal = "1.0.0"
pcf8563 = { git = "https://github.com/invpt/pcf8563-rs.git", branch = "main" }
sntpc = { version = "0.10.1", default-features = false, features = ["log"] }
chrono = { version = "0.4.43", default-features = false }
chrono-tz = { version = "0.10.4", default-features = false }
micromath = { version = "2.1.0", default-features = false, features = [
    "num-traits",
] }
embassy-futures = { version = "0.1.2", features = ["log"] }
sntpc-net-embassy = { version = "0.10.0", features = ["log"] }

[profile.dev]
# Rust debug is too slow.
# For debug builds always builds with some optimization
opt-level = "s"

[profile.release]
codegen-units = 1        # LLVM can perform better optimizations using a single thread
debug = 2
debug-assertions = false
incremental = false
lto = 'fat'
opt-level = 's'
overflow-checks = false
